version: 0.0
os: linux
files:
  - source: /cluster_create.yml
    destination: /
hooks:
  BeforeInstall:
    - location: scripts/install_dependencies
      timeout: 300
  AfterInstall:
    - location: scripts/start_server
      timeout: 3000
